#Circleci Configuration file

version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.11.1
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD
    
    steps:
      - checkout:
          path: cd /tests/test_cases
      
      - run: echo "circleci is fun"

  integration:
    docker:
      - image: cimg/python:3.11.1
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD
    
    steps:
      - run: echo "Good to see you"
    
workflows:
  build_and_integration:
    jobs:
      - build
      - integration

      
# VS Code Extension Version: 1.5.1